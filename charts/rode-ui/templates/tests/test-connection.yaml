apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "rode-ui.fullname" . }}-test-connection"
  labels:
    {{- include "rode-ui.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "rode-ui.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
