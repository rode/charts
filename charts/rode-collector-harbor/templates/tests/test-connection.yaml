apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "rode-collector-harbor.fullname" . }}-test-connection"
  labels:
    {{- include "rode-collector-harbor.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "rode-collector-harbor.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
